{
    "oci-role-based-policy-document": {
       "name": "OCI-Operator-<ENVIRONMENT>_policies",
       "description": "The policy set defined for role OCI-Operator includes group, and dynamic group based policies",
       "version": "2.5.1",
       "role": "OCI-Operator",
       "environment": "ENVIRONMENT",
       "domain": "DOMAIN",
       "group-based-policies": {
            "policy": [
               "Allow group OCI-Operator-<DOMAIN> to inspect all-resources in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read instances in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read audit-events in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to inspect alarms in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read metrics in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use ons-topics in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use ons-subscriptions in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use cloud-shell in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use repos in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read objectstorage-namespaces in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to inspect logging-family in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use functions-family in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use apm-domains in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read vaults in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use keys in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to inspect cloudevents-rules in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use buckets in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use objects in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use file-family in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to read mount-targets in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use volume-family in compartment <ENVIRONMENT>",
               "Allow group OCI-Operator-<DOMAIN> to use instance-family in compartment <ENVIRONMENT>"
            ]
        },
        "dynamic-group-based-policies": {
            "policy": [
                 "Allow dynamic-group OCI-Operator-DG-<DOMAIN> to use instances in compartment <ENVIRONMENT>"
             ]
         },
         "other-compartment-based-policies": {
            "policy": [
                "Allow group OCI-Operator-<DOMAIN> to use virtual-network-family in compartment network"
            ]
         }
    }
 }
